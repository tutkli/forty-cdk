---
paths:
  - '**/*.spec.ts'
  - 'projects/forty-cdk-harness/**'
  - 'vitest.config.ts'
  - 'playwright.config.ts'
  - 'projects/forty-cdk/src/test-utils/**'
---

# forty-cdk — testing conventions

Detailed companion to the root `CLAUDE.md`. These auto-load when you edit a spec, the harness app, the Vitest / Playwright configs, or the test utilities.

## Vitest builder & setup-file invariants

The `@angular/build:unit-test` builder is configured in `angular.json` to load `vitest.config.ts` from the repo root (`runnerConfig: true`) and to run `projects/forty-cdk/src/test-utils/vitest-invariants-setup.ts` before every spec (`setupFiles`). Together they enforce Vitest's mock-reset / unstub invariants (`clearMocks`, `restoreMocks`, `unstubGlobals`, `unstubEnvs`) so `vi.fn()` call history, `vi.spyOn` patches, and `vi.stubGlobal` / `vi.stubEnv` are reset at the test boundary without relying on per-spec discipline. The setup-file layer is the load-bearing one — at the time of writing the builder does not propagate user `test.*` invariants to the runtime config — and `vitest.config.ts` documents the intended invariants in one canonical place.

The same dual-layer (config declaration + setup-file twin) carries the **`testTimeout: 15000`** budget — raised from Vitest's 5000ms default. The floating-ui overlay suites (select / combobox / popover / …) drain several real macrotask hops per `flush()`; under the default parallel jsdom schedule a CPU-starved worker can stall a single hop past 5000ms on an otherwise-correct test (select.spec runs ~7x slower under full-suite contention than in isolation, which is what surfaced the timeout flake). Because the builder doesn't propagate `test.testTimeout` either, the load-bearing copy is the top-level `vi.setConfig({ testTimeout: 15000 })` in `vitest-invariants-setup.ts` (it runs once per spec file before its tests). 15000ms absorbs the contention spike without masking a genuine hang — a truly stuck test still fails, just later.

## Nightly scheduler-hostile profile

A second, scheduler-hostile profile runs nightly via `.github/workflows/test-shuffle.yml` to surface leaks the default isolated schedule masks (polyfills, live regions, fake timers, ad-hoc body children). It declares `pool: 'forks'` + `singleFork: true`, `isolate: false`, `fileParallelism: false`, and `sequence.shuffle.{files, tests}` — gated behind `FORTY_CDK_TEST_WORST_CASE=true` so the default `pnpm test` path stays byte-identical. On `@angular/build@21.2.9` the builder propagates `pool` and `isolate` through to the Vitest runner but strips `fileParallelism` and `sequence.*`; the latter activate automatically the day the builder propagates user config to the runner level (see the comment block in `vitest.config.ts`). Reproduce the nightly locally with `FORTY_CDK_TEST_WORST_CASE=true pnpm test` (Bash) or `$env:FORTY_CDK_TEST_WORST_CASE='true'; pnpm test` (PowerShell). The nightly workflow is non-blocking for PRs — a red run is a signal to investigate against the `### Test isolation — non-negotiables` checklist, not a merge blocker.

## Testing notes

Vitest runs through the Angular CLI builder `@angular/build:unit-test` (configured in `angular.json`). The spec tsconfig (`projects/forty-cdk/tsconfig.spec.json`) sets `types: ["vitest/globals"]`, so `describe` / `it` / `expect` are global — no imports needed. `jsdom` is the DOM environment. Tests use modern standalone `TestBed` setup; a single placeholder spec exists at `projects/forty-cdk/src/lib/forty-cdk.spec.ts` as reference.

### The suite runs zoneless — the invariant is asserted once

`renderHost` (`src/test-utils/render.ts`) configures every fixture with `provideZonelessChangeDetection()`, so **the whole suite already runs zoneless** and the invariant is pinned in one place: [`src/test-utils/render.spec.ts`](../../projects/forty-cdk/src/test-utils/render.spec.ts). That file asserts the provider is installed (`ɵPROVIDED_ZONELESS` / `ɵZONELESS_ENABLED`, the flag Angular's own scheduler reads to decide it must not depend on `NgZone`) and, separately, the behaviour it buys — a post-mount signal write reaching the DOM after a bare macrotask hop, with no `detectChanges()` and no `flush()`.

**Do not write a per-primitive `describe('zoneless reactivity')` block.** Until [#1595](https://github.com/tutkli/forty-cdk/issues/1595) the rule here demanded one and 84 of them shipped, asserting nothing their neighbours did not: with no zone-based configuration anywhere in the suite there is nothing for such a block to contrast against, so every one of them would have passed under any other title. Worse, they read as a claim that the _other_ tests do not cover zoneless — which is false, and misleads anyone reasoning about coverage. Seventy-two of them were retitled after what they verify (mostly `reactive updates`: a signal write after mount reaching the DOM), and the 22 cases whose assertions a neighbour already made were deleted — taking the other twelve blocks with them, since those cases were all the block held. The bar for deleting was a sibling in the same file that mounts the same fixture, fires the same trigger, and asserts everything the case did; anything whose conjunction of claims no single neighbour made was kept.

The same sweep took the framing out of the **107 scattered `it` titles** that carried it outside those blocks — `zoneless: …`, `… without Zone.js`, `… under provideZonelessChangeDetection`, `… (zoneless change detection)` — so the rule generalises past the `describe` shape: **no test title claims a zoneless contrast, because there is none in this suite to claim.** Where the framing _was_ the whole title (`works under provideZonelessChangeDetection`) the test was renamed after its body; naming two of them surfaced a verbatim twin in `src/lib/toast-over-modal.spec.ts`, deleted under the same bar. Four titles keep the word: three name the geometry **dead zone** (pane-resizer, table column resize), and `per-primitive-defaults.spec.ts` keeps `under an explicit provideZonelessChangeDetection()` because an explicitly-provided scope is that case's _subject_ rather than a contrast. A new primitive owes the same thing every primitive owes — behaviour, a11y, and the SSR fixture below — under a title that names it.

The rule worth keeping is the narrow one: **a spec that bypasses `renderHost` and builds its own `TestBed` must provide zoneless change detection itself**, because `render.spec.ts` cannot speak for a fixture it did not create. Note the guarantee is doubly held today — `TestBed` base-provides `provideZonelessChangeDetectionInternal()` on its own, so removing the call from `renderHost` fails only the provider assertion and not the behavioural one — which is exactly why the provider assertion exists: it pins the intent regardless of what the framework happens to default to.

### Shared contract suites — a new primitive adopts every one that applies

`src/test-utils/contract/` holds the assertions that are **identical across a family of primitives**, as `assert*Contract` functions each opening their own `describe`. A primitive calls the ones its anatomy matches from its own spec; there is no inheritance and no base class, so partial adoption (a subset of `flags`, one vocabulary of two) is the normal shape. All five are internal to the spec suite and never re-exported from `public-api.ts`.

| Contract                                                                             | Family                                                           | Adoption is enforced by                                                                                                          |
| ------------------------------------------------------------------------------------ | ---------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| `assertRovingTabindexContract`                                                       | primitives owning a roving-tabindex keyboard model               | the file header's exclusion list (Menu, drag-drop), read by hand                                                                 |
| `assertFormControlContract`                                                          | `FormValueControl` / `FormCheckboxControl` implementors          | [`form-control-adopters.spec.ts`](../../projects/forty-cdk/src/lib/form-control-adopters.spec.ts) — source-derived, no allowlist |
| `assertDismissibleLayerContract`                                                     | the primitives exposing a `dismissible` input                    | the file header, read by hand                                                                                                    |
| `assertOverlayTriggerAriaContract`                                                   | trigger-anchored overlays (plus Dialog / Drawer triggers)        | the file header, read by hand                                                                                                    |
| `assertDataStateContract` ([#1597](https://github.com/tutkli/forty-cdk/issues/1597)) | every piece binding `'[attr.data-state]'` to a literal value set | [`data-state-adopters.spec.ts`](../../projects/forty-cdk/src/lib/data-state-adopters.spec.ts) — source-derived, no allowlist     |

**The two source-derived guards are the shape to copy for a sixth contract**, because a missing adopter is otherwise _invisible_: the suite reports N green primitives whether the roster lists N or N + 1, which is how the hand-maintained form-control roster silently lost four. Each derives the family from library source and fails on an entry point that declares itself into it without calling the contract. The `data-state` one folds the **same** `host: { … }` extraction `scripts/lib/convention-matrices.mjs` runs to generate the `data-state` rows of the matrices in `.claude/rules/conventions.md` — so `pnpm check:matrices` fails on the table a new vocabulary would change, and the spec fails on the family nothing documents. Neither gate can be satisfied alone.

Three things about the `data-state` contract in particular:

- **It covers a _literal_ value set only.** A `data-state` delegated to a signal or method keeps its vocabulary in the delegate's own type, which no source scan can read — the same boundary the matrix draws with its `data-state` computed by a signal / method row. An entry point on both sides of it (Drawer, NavigationMenu, Stepper, Tree) adopts for its literal pieces and is covered on their behalf; Checkbox, Progress, ScrollArea and Table are outside it entirely.
- **Every piece a mount factory reports must be in the same state at the same time**, because the contract's whole claim is that the pieces of one primitive speak one vocabulary. So a mount reports the trigger whose state it drives and not its siblings, and "exactly one active, the rest inactive" stays with the primitive's own suite — that is a single-selection claim, not a vocabulary one. Two vocabularies in one entry point are two calls, distinguished with `options.label`.
- **Adoption is enforced _per entry point_, so a covered entry point does not imply every one of its pieces is reported.** The guard asks whether the entry point calls `assertDataStateContract` at all, which the `menu` entry point satisfied with its `[forMenuItemIndicator]` `checked` / `unchecked` call while `[forMenu]`'s own root `data-state` was asserted by nothing on either channel ([#1645](https://github.com/tutkli/forty-cdk/issues/1645)). No source scan can do better: a `pieces` key is a runtime selector, with nothing tying it back to the source file that emits the binding. So the completeness of a `pieces` resolver is a review question rather than a gated one — read the entry point's own generated `data-state` matrix rows against what its resolvers report, and give a piece the resolver leaves out a hand-written `it` of its own.

### SSR smoke coverage — every new primitive needs a fixture

The library advertises zoneless **and** SSR-safe primitives, so server-side coverage is part of every new primitive's workflow — not an afterthought. A primitive with no SSR assertion can ship a `document` / `window` access (or an eager DOM mutation, or shared module-level state between requests) that only fails in a server render, undetected until an Angular Universal consumer hits it.

`projects/forty-cdk/src/lib/ssr/ssr.spec.ts` is the single suite that proves this, and it is **sweep-first** ([#1594](https://github.com/tutkli/forty-cdk/issues/1594)): the fixtures live in `src/lib/ssr/fixtures/*.ts` grouped by family, every one of them is registered once in [`fixtures/registry.ts`](../../projects/forty-cdk/src/lib/ssr/fixtures/registry.ts), and the parameterised cases iterate that registry. **A new primitive owes a fixture component and a registry entry — never a new hand-written `it`.**

Six sweeps run over the registry, and the reason there are six is that each is blind to what the next one catches:

| Sweep                                   | Asserts                                                                                                                            | Blind to                                                             |
| --------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| renders without throwing                | the primitive constructs and paints server-side at all                                                                             | markup that renders and says nothing                                 |
| emits the markup hydration matches on   | the `role` / `aria-*` / `data-state` set is non-empty, plus the entry's declared `markup`                                          | anything the entry did not declare                                   |
| leaves `<body>` untouched (`open` only) | no portal appends a child, no scroll lock writes `style`, no `InertSiblingsStack` mark on the fixture's own branch                 | a side effect that only subscribes; a re-parent _inside_ the fixture |
| appends nothing to `<head>`             | no document-level stylesheet — `[forScrollAreaViewport]` injects one from its **constructor**, so no `open` state gates it         | every escape that is not a `<head>` child                            |
| installs no global listener / no timer  | the escapes a `<body>` snapshot cannot see, since jsdom hands the server render a real `document` and `window`                     | markup, and anything that neither listens nor schedules              |
| resolves no floating position           | no inline `translate` / `clip-path` and none of the positioners' `--for-*` properties, so `@floating-ui/dom` provably never starts | everything else                                                      |

Two things about the `<body>` sweep are worth knowing before trusting it. Its baseline is taken **before** `createComponent` (excluding the branch `TestBed` appends), so a mutation from a directive **constructor** — which runs in the template's creation pass, before any `detectChanges()` — is inside the window. And that exclusion is its blind spot, in exactly the shape of the third escape: `InertSiblingsStack` marks the portal root's **siblings**, which here is either the excluded `TestBed` branch or, for a `[container]`-scoped modal, a node inside the fixture host that is no `<body>` child at all. So `inert` is asserted separately over the host's ancestry — not as a blanket `[inert]` count, because the always-mounted panel family reflects `aria-hidden` + `inert` legitimately and an open-state fixture renders it.

The `<head>` sweep needs one piece of hygiene the others do not: a document-level injection is idempotent by id and survives `resetTestingModule()`, so the suite's `afterEach` removes whatever a test added to `<head>`. Without that, the first fixture to inject would leave the sweep a false negative for every fixture after it.

A registry entry is `{ component }` plus three optional fields:

- **`open: true`** — the fixture mounts in its open / active state, so the browser-only side effects (portal, scroll lock, inert siblings, dismissible layer, focus trap, floating positioner, autoplay interval) would run if their `isPlatformBrowser` gate were missing. The last three sweeps plus the `<body>` one iterate exactly these; a closed overlay proves nothing there.
- **`markup`** — per-element expectations resolved inside the fixture host: `attributes` (exact value, `null` asserting absence), `present` (non-empty, whatever the value — so a **boolean** `data-*` / `hidden`, emitted empty by convention, goes in `attributes` as `''` instead), `pairs` (the attribute must equal the `id` of another selector's element), `within` (the element must stay a descendant of another). `select` resolves the **first** match, so an nth-element claim is one of the hand-written cases below. **`pairs` is what makes the sweep able to fail**: it is the trigger↔surface `aria-controls` / label↔control `aria-labelledby` that hydration re-resolves on the client, and dropping one server-side is exactly the failure [#1409](https://github.com/tutkli/forty-cdk/issues/1409) (tabs shipped no trigger↔panel pairing) shipped past a green suite. Without a `markup` entry a fixture only proves it rendered and emitted _some_ wiring, so declare the pairings your primitive resolves server-side.
- **`noWiring: '<why>'`** — the primitive emits no `role` / `aria-*` / `data-state` server-side, and the string says why (inline style only, native tag semantics, mounted closed). The absence is **asserted**, not skipped, so a primitive that should wire ARIA cannot be quietly parked here and one that shouldn't cannot start emitting.

The hand-written cases that remain are the ones the declarative shape cannot state: a **count** ("exactly one `aria-current="page"`", "three rows"), a **loop over pairs** (every tabs / stepper trigger↔panel, not just the first match a `select` resolves), an **nth element** (the second tabs trigger's consumer-set static id), an **inequality** (two `[forMenu]` openers must not share an id), an **inline style** (VisuallyHidden's clip, the virtualizer's sizer height), a **substring** (`aria-describedby` containing both the description and the error id), a **spy** (`BodyScrollLock` is a no-op; the root singletons are per-bootstrap), or a claim **no sweep can make at all** — of which there is one, and it carries a comment saying so. That one is the NavigationMenu viewport: re-parenting `[forNavigationMenuContent]` into `[forNavigationMenuViewport]` is a move _within_ the fixture tree, so `<body>` is untouched, no listener is installed and no position resolves — all four open-state sweeps stay green whichever way it went, and only the content's parent chain tells them apart.

The twenty "does not portal or mutate `<body>`" cases the sweep replaced were in none of those categories: under `ngServerMode` every `afterNextRender` is inert by construction, so all twenty passed for one shared reason and none could fail independently. **Write a single-element attribute or pairing claim as a `markup` entry, not as a new `it`** — that is the rule the twenty broke.

**The whole suite runs under real server mode, so `afterNextRender` never fires** ([#1506](https://github.com/tutkli/forty-cdk/issues/1506)). Mocking `PLATFORM_ID` alone — all the suite did until then — left the render lifecycle running, so a primitive whose static markup depended on an `afterNextRender` callback _having run_ passed for a reason production never provides. That is exactly how [#1377](https://github.com/tutkli/forty-cdk/issues/1377) (stepper painted the completed state server-side) and [#1409](https://github.com/tutkli/forty-cdk/issues/1409) (tabs shipped no trigger↔panel ARIA pairing) shipped. The suite-level `beforeEach` therefore sets four things, and a fixture author needs to know all four:

- `globalThis.ngServerMode = true` — the global Angular gates its render hooks on. `afterNextRender` / `afterEveryRender` are genuine no-ops, as on Angular Universal. **Anything your primitive needs in the pre-hydration DOM must be resolved without them** — register synchronously and guard the lookup (see `unsetInput` / `isUnset` for the unwritten-binding case) rather than deferring.
- `PLATFORM_ID` → the server id, so every `isPlatformBrowser` gate reports server.
- The browser globals a Node server does not have (`requestAnimationFrame` / `cancelAnimationFrame`, `matchMedia`, `IntersectionObserver`, `ResizeObserver`, `MutationObserver`) are stubbed to `undefined` for the suite. jsdom supplies all of them, so an ungated access used to pass here and fail only on a consumer's server; now it throws in the fixture's own render case.
- **`Element.prototype.getBoundingClientRect` is made to throw** ([#1636](https://github.com/tutkli/forty-cdk/issues/1636)). Same gap as the row above, one rung more dangerous: the globals are constructors and schedulers, whereas a layout read returns a _plausible_ value. A rect needs a layout engine, and domino — the DOM `@angular/platform-server` bundles — defines the method nowhere, so an ungated read is a `TypeError` on Angular Universal while jsdom hands back a rect full of zeros. **A geometry read must be gated on `isPlatformBrowser`, not on whether its subject happens to resolve**: both #1636 reads (`[forNavigationMenuIndicator]`, `[forNavigationMenuViewport]`) measured a _registered_ trigger / panel, so a registry that was empty server-side was the only thing protecting them — and registering synchronously exposed both at once. The stub carries a liveness assert, because which prototype owns the descriptor differs by platform ([#193](https://github.com/tutkli/forty-cdk/issues/193)) and a run that patched none would report nothing.

The listener / timer sweeps over the open-state entries are what back that profile up: no `document` / `window` listener is installed, and **no timer is scheduled** — every `setInterval`, plus every `setTimeout` carrying a delay. The delay filter is the one carve-out left: Angular's zoneless scheduler always schedules one bare `setTimeout(cb)` per render, and there is no way to tell it from a library call that also omitted its delay. Every timer the library schedules passes an explicit delay (`LiveAnnouncer`'s deferred write passes `0`), so the sweep is blanket in practice — **keep it that way when you add one.**

When you add a primitive, add its SSR fixture in the same PR:

1. Add a minimal `@Component` fixture — the root plus the one or two pieces needed to render — to the `fixtures/*.ts` file for its family, and register it in `SSR_FIXTURES`. That one entry is what puts it in every sweep.
2. If the primitive portals content, runs `afterNextRender` side effects, or otherwise gates on `isPlatformBrowser` (every overlay, plus disclosure / menu families with viewport or portal side effects), mount the fixture in its **open / active** state and flag the entry `open: true`. A closed fixture for such a primitive proves little, because the gated side-effect path never runs — and without the flag the four open-state sweeps never see it.
3. Declare the primitive's server-side ARIA pairings in `markup`. This is the step that turns "it rendered" into "it rendered the wiring hydration needs", and skipping it is how a primitive ends up asserting only that it did not throw.
4. Date / time primitives need a `DateAdapter`; provide `...provideNativeDateAdapter()` in the fixture's `providers`, as `CalendarFixture` / `DateFieldFixture` already do.

If a new fixture throws on the server, or mutates `document.body` in its open state, that is a genuine SSR bug in the primitive — fix the primitive (gate the offending access behind `isPlatformBrowser` / move it into `afterNextRender`), not the test. Same for the sweeps: a fixture that schedules a timer, installs a listener, or resolves a position server-side is a missing gate, never a sweep to relax.

#### Hydration is a second suite, not a fixture in this one

`projects/forty-cdk/src/lib/ssr/incremental-hydration.spec.ts` ([#1582](https://github.com/tutkli/forty-cdk/issues/1582)) covers what a server render alone cannot: whether the markup the server emitted still means the same thing **after the client claims it**. It runs a real round trip — `renderApplication` from `@angular/platform-server`, the emitted HTML installed into the live jsdom document, then `bootstrapApplication` with `provideClientHydration(withIncrementalHydration())` — so `@defer (hydrate …)` blocks are genuinely dehydrated and genuinely hydrate on their trigger. The harness is `src/test-utils/hydration.ts`.

It has to be its own file because the smoke suite's profile is the exact opposite: that one pins `ngServerMode = true` and stubs the browser globals to `undefined` for the whole file, which is precisely what a client hydration must not see. Two conventions keep the new suite honest:

- **`hydrate never` for any assertion about the dehydrated state, `hydrate when` for the one trigger a test fires.** Both are deterministic — a timer or viewport trigger would race the assertion instead of the DOM. The `hydrate when` expression reads a signal on the fixture, which `renderThenHydrate` hands back as `instance`.
- **`settleHydration(appRef)` is the drain, and it must be awaited** (`forty-cdk/no-floating-flush` covers the name). It is the `ApplicationRef` counterpart of `flush(fixture)` — there is no `ComponentFixture` here — and `hydration.ts` joins `flush.ts` as the second file exempt from `forty-cdk/no-bare-whenstable`.

A primitive does **not** owe this suite a fixture the way it owes the smoke suite one: what the four disclosure cases and the virtualized listbox case pin is the library-wide id-adoption contract, not per-primitive markup. Add a case here when a primitive introduces a _new kind_ of cross-piece reference (a registry-resolved id, an id cached outside the handle it names), because that is what the contract in `.claude/rules/conventions.md` is stated over.

### Test isolation — non-negotiables

These invariants are the rationale behind the mechanical enforcement (ESLint rules, Vitest setup file). They exist because each one was, at some point, a bug that bled state across specs or a contract leak that made a refactor harder than it needed to be. A new spec must clear them all.

1. Every `vi.useFakeTimers()` has a matching `vi.useRealTimers()` in `afterEach` of the same `describe` — never inline at the end of an `it`. Inline restores leak timers when the `it` fails before reaching the call.
2. Every `globalThis` polyfill (observers, `fetch`, `matchMedia`) has a matching `afterAll` restore. Prefer the `installObserverPolyfills()` helper in `projects/forty-cdk/src/test-utils/observers.ts` — it already pairs install with restore.
3. Every `addEventListener` installed by test code (not by the directive under test) has a matching `removeEventListener` in `try/finally`. A throwing assertion mid-test must not leave a global listener attached for the next spec.
4. Every `appendChild` to `document.body` from test code is removed in `afterEach` (or `try/finally` in the same `it`). The `TestBed` fixture host is cleaned up for you; ad-hoc body children are not.
5. Overlay specs (any primitive that portals content to `document.body`) call `afterEachOverlayCleanup()` from `projects/forty-cdk/src/test-utils/overlay-cleanup.ts`. It is a leak detector for failing-mid-render scenarios — without it a thrown assertion can orphan the portal and the next spec sees stale ARIA. Enforced by `forty-cdk/require-overlay-cleanup` (fires when a spec imports a maintained portaling symbol — a content directive like `ForPopoverContent` / `ForSelectContent`, or an imperative manager like `ForDialogManager` — but the file never calls `afterEachOverlayCleanup()`; keyed on the content directive / manager rather than the root coordinator so contract specs importing `ForPopover` / `ForSelect` without opening the overlay aren't flagged, and the SSR smoke suite is exempt because it renders server-side and portals nothing).
6. **No reading directive internal signals from a spec.** The contract is the DOM: ARIA attributes, `data-state`, `data-side`, focus, host-bound classes. Accessing `directive.signalX()` is implementation leakage that locks the directive's private shape into the spec; assert against the rendered DOM instead.
7. `fixture.whenStable()` is used only inside `projects/forty-cdk/src/test-utils/flush.ts`. In specs, always `await flush(fixture)` — it is the canonical waiter and is the only place where the underlying API may change without churning every spec.
8. Geometry assertions (measured dimensions and math derived from them) run in Playwright, not Vitest. Vitest covers wiring (listener attached, callback fired, signal updated) without faking measurements. See the `### E2E (Playwright)` subsection below and [#195](https://github.com/tutkli/forty-cdk/issues/195) for the full rationale and the `*.prototype.getBoundingClientRect` cross-platform trap. A spec that stubs a **single element's** `getBoundingClientRect` to exercise wiring (how many times the rect is read; which pointer id may drive a session — `slider.spec.ts`'s `#1153` / `#1228` blocks) may keep the stub, but must assert only call counts and _relative_ value transitions (changed / unchanged). The moment it asserts a concrete value the stub's numbers produced, it is checking the arithmetic against its own mock — the tautology [#1397](https://github.com/tutkli/forty-cdk/issues/1397) item 5 removed.
9. E2E selectors use `data-testid="…"`. A static `id` on a `[id]`-binding directive is now **preserved**, not shadowed ([#659](https://github.com/tutkli/forty-cdk/issues/659) — the directive adopts the consumer's static `id` instead of clobbering it), so a `#id` selector is no longer silently broken. `data-testid` stays the convention regardless: it keeps test hooks decoupled from the aria-wiring `id` (which a directive may still own and re-emit) and from the static-only boundary (a `[id]="expr"` property binding is _not_ adopted).
10. `expect(x).not.toBeNull()` followed by `x!.foo` is noise — drop the assertion. The non-null assertion is already telling the reader (and the compiler) what you know; doubling it adds nothing, and the `!` throws on a null just as loudly. Enforced by `forty-cdk/no-redundant-not-tobenull`, which fires only when the same identifier is `!`-asserted within the next few statements — a standalone `expect(x).not.toBeNull()` that _is_ the test stays legal.
11. Placement / direction assertions use concrete values (`.toBe('top')`, `.toBe('rtl')`), not `.toBeTruthy()`. A truthy check passes for the wrong value just as eagerly as the right one.
12. Every `flush(fixture)` / `flushPositioning(fixture)` / `nextMacrotask()` / `settleHydration(appRef)` call is `await`ed (the free function from `test-utils/flush.ts` — or `test-utils/hydration.ts` for the last, the `ApplicationRef` drain a hydration spec uses in place of a fixture — or the `flush` method destructured from `renderHost()`). All four return `Promise<void>`: a bare `flush();` statement runs only the initial synchronous `detectChanges()` and lets the async drain (`whenStable` → macrotask → second `detectChanges`, plus `afterNextRender` / floating-ui positioning) escape the test boundary, so assertions can pass against stale DOM. Enforced by `forty-cdk/no-floating-flush` (fires on a floating call — one that is the whole expression of an `ExpressionStatement`; `await`/`return`/`void`/assignment/`.then(…)` are all consumed and allowed).
13. **A spec carries no unused import, local or destructured binding** — `@typescript-eslint/no-unused-vars` applies to specs on the same terms as library source, `^_` being the escape hatch for a binding that exists only to be type-checked (`const _row = ctx.$implicit` beside an `Equal<typeof _row, …>` assertion in `table-body.spec.ts`). The rule was `off` for `**/*.spec.ts` until [#1595](https://github.com/tutkli/forty-cdk/issues/1595), relaxed for a reason that turned out not to hold: "specs frequently destructure for asserts only" described 19 sites across 220 files, all of them dead bindings rather than a pattern worth protecting. What the relaxation actually cost was **unused-import detection** — deleting a `describe` block leaves its imports behind, `pnpm lint` and `pnpm typecheck` both stay green, and hand-counting occurrences misses any symbol that is also a destructured name (`flush` is both an import from `test-utils/flush.ts` and a member of `renderHost()`'s result, which is how four orphans survived a manual sweep in #1595). Turning the rule back on found 32 violations: twelve dead imports, a 33-line fixture component nothing referenced, seventeen dead destructured bindings, and the two `_row` locals above.
14. **A `detectChanges()` may not stand in for a `flushPositioning()`.** Rule 12 covers the waiter that _was_ written; this one covers the waiter that was not. `detectChanges()` runs the synchronous render — under zoneless it is `ApplicationRef.tick()`, so component effects and `afterNextRender` callbacks do run — and then returns. What it does **not** do is drain the promise / RAF hops behind `@floating-ui/dom`: `computePosition` resolves across several microtask hops and `autoUpdate`'s RAF polyfill in jsdom is a `setTimeout`. So an assertion on the positioner's output placed after a bare `detectChanges()` reads the **previous** position — or the anti-flash baseline — and passes for a reason production never provides. `forty-cdk/no-bare-detect-changes` fails `pnpm lint` on that pairing: a `detectChanges()` whose following statements (siblings in the same block, up to the next `detectChanges()` or waiter call, `LOOKAHEAD` 10) contain an `expect(` **and** one of the positioner marks — inline `translate` / `clip-path`, `data-side` / the resolved-placement `dataset` entry, or one of the positioner-written `--for-*` properties (`--for-anchor-*`, `--for-available-*`, `--for-arrow-offset`, `--for-content-transform-origin`). The mark list is not invented for the rule: it is the set [`flushPositioning`'s own JSDoc](../../projects/forty-cdk/src/test-utils/flush.ts) already names, which is what keeps the rule and the waiter from drifting apart. Detection scans the whole window rather than only the lines carrying `expect(`, so aliasing the value out first (`const t = el.style.translate;` … `expect(t)`) does not evade it — at the cost of firing on a setup read followed by an unrelated assertion, the cheap direction to be wrong in.

    **The escape hatch is `@sanctioned-sync-render(<subject>): <why>`**, a line comment on the line(s) immediately above the call, anchored like its `@sanctioned-effect` / `@sanctioned-pull` cousins ([#1606](https://github.com/tutkli/forty-cdk/issues/1606): only a line comment whose text _starts_ with the phrase is a marker, so a JSDoc block quoting it licenses nothing, and a bare `@sanctioned-sync-render` with no parenthesised subject is reported as malformed). It anchors **more tightly** than they do, on the comments directly above the call (`getCommentsBefore`) rather than on any comment within six lines of it, and that difference is not stylistic: an `effect(` opens a multi-line body that holds two candidate sites apart, whereas a `detectChanges()` is one line, so a marked call and an unrelated second one fit inside a six-line window trivially — `floating.spec.ts` already has the shape (marker → call → two assertions), one added render away from licensing a stale assertion in silence. Adjacency is structural, so a marker cannot reach past the code in between; the line window survives only to keep "immediately above" literal when blank lines intervene. `badMarkerDoesNotCarryOver` in the fixture is the executable form of it. It exists because the **un-drained render is sometimes the subject of the test**: the three sites in [`core/floating/floating.spec.ts`](../../projects/forty-cdk/core/src/floating/floating.spec.ts) assert what the surface wears in the window _before_ a position resolves — the armed baseline, the re-armed baseline at the retained stale position, and the `clipUntilPositioned=false` opt-out's claim that no baseline is armed there at all — none of which is observable through a drain. `grep @sanctioned-sync-render` is the complete ledger, and it is three entries.

    **The rule keys on the pairing rather than on the call, and that is the load-bearing design decision.** [#1596](https://github.com/tutkli/forty-cdk/issues/1596) proposed banning `detectChanges()` outright outside `test-utils/`, with a marker for the synchronous-host-binding case. The audit it asked for is what argues against that shape: of the **739** bare `detectChanges()` calls in the suite, **736** assert on DOM the synchronous render itself produces — a host or template binding (`'[style.--for-carousel-offset]'`, `'[style.touch-action]'`, `[style.grid-template-columns]`, `[style.height.px]`), an ARIA attribute, `data-state`, or a `document.body` child appended by the imperative handler a line above (`createDragPreview`'s `doc.body.appendChild`) — or are the SSR smoke suite's **absence** assertions, where `ngServerMode = true` means no deferred hook fires by construction. A blanket ban would therefore mean ~736 escape-hatch markers, and a 736-entry ledger is not a ledger: the `@sanctioned-*` families work here precisely because grepping one returns three or four or six sites a reviewer actually reads. The **three** sites the pairing rule finds are the whole risk surface, and they are all deliberate.

    Two corollaries. **The audit is not a one-off**: what makes it stay true is the rule, so a new positioning assertion cannot reach `main` on a synchronous render without either a waiter or a marker. And **`flush` is not made the only exported waiter** (#1596's fourth proposal): `renderHost` hands back the whole `ComponentFixture` because every spec needs it — for `componentInstance`, `nativeElement`, `destroy()` — so `fixture.detectChanges` is reachable no matter what the result object also exposes, and the 736 legitimate calls are the reason to keep it reachable rather than to hide it.

### E2E (Playwright)

The Vitest + jsdom suite is the contract layer for ARIA, signals, and the data-state vocabulary, but jsdom mis-models `document.activeElement`, `inert`, and the focus-event order. Real focus management — focus trap, return focus, vetoable `autoFocusOnOpen` / `autoFocusOnClose`, layered Escape, click-outside, disabled-skip in keyboard navigation — runs against real browsers via Playwright.

jsdom returns zeros for layout APIs (`getBoundingClientRect`, `offset*`, `client*`, `scroll*`) and does not run CSS. Geometry-driven assertions — anything that exercises measured dimensions, math derived from dimensions (snap percentages, `closeThreshold * dim`, `'NNpx'` conversion), CSS custom properties populated from `getBoundingClientRect`, or `IntersectionObserver` outcomes — runs against real browsers via Playwright. The Vitest layer asserts wiring (listener attached, callback fired, signal updated) without faking measurements; do not stub `*.prototype.getBoundingClientRect` or any other layout API. The `*.prototype.getBoundingClientRect` pattern in particular is cross-platform fragile: Linux jsdom defines the descriptor on `HTMLElement.prototype` while macOS/Windows jsdom only defines it on `Element.prototype`, and a single-prototype patch is silently shadowed on the rung the runtime actually consults — see [#193](https://github.com/tutkli/forty-cdk/issues/193) for the trail. The library's harness app exposes a per-fixture query-driven viewport (e.g. `drawerHeight`) plus a capturing `ErrorHandler` (records throws onto `window.__fortyCdkHarnessErrors`) so geometry-throwing paths can be asserted from Playwright.

- `playwright.config.ts` (root) declares four projects — desktop `chromium` + `webkit`, plus the two `@mobile`-filtered emulated ones documented below — running in parallel with `webServer: 'ng serve forty-cdk-harness --port 4400'`. The port is overridable via the `FORTY_HARNESS_PORT` env var (`const PORT = Number(process.env['FORTY_HARNESS_PORT']) || 4400`); default behaviour stays byte-identical at `:4400`. Use it when two checkouts run E2E concurrently (the main repo plus a `git worktree`): because `reuseExistingServer` is `true` locally, a second run would otherwise reuse the first checkout's `:4400` server and silently exercise the _other_ worktree's sources — a brand-new harness route from the change under test just 404s. Start your own harness on a free port and point Playwright at it: `FORTY_HARNESS_PORT=4410 pnpm exec ng serve forty-cdk-harness --port 4410` then `FORTY_HARNESS_PORT=4410 pnpm exec playwright test …`.
- The harness app under `projects/forty-cdk-harness/` is dev/CI-only. It reads the library by sources via per-entry-point `paths` overrides in its `tsconfig.app.json` (`forty-cdk/<primitive>` → `../forty-cdk/<primitive>/src/public-api.ts`, plus `forty-cdk/core`), so changes to library code show up without rebuild.
- Per-primitive fixtures live in `projects/forty-cdk-harness/src/app/fixtures/<primitive>.fixture.ts`, mounted on routes `/<primitive>`. A primitive may own several — `table`, `table-virtualized`, `table-reorder`, `for-table-body-*` are all separate routes — and the variant fixtures are named for the axis they add rather than for the primitive alone. Composition contracts get their own route too: `/nested` covers the popover-inside-dialog Escape-stack, `/toast-over-dialog` the toast-over-modal marker, `/overlay-inside-dialog` and `/tooltip-over-menu` the remaining layer pairings.
- E2E specs live in `projects/forty-cdk-harness/e2e/<primitive>.e2e.ts`. They use `data-testid="…"` rather than `id="…"` for any element bound to a directive. A static `id` is now preserved on `[id]`-binding directives (`forPopoverTrigger`, `forSelectOption`, `forComboboxInput`, etc. adopt it — [#659](https://github.com/tutkli/forty-cdk/issues/659)), so a `#id` selector works; `data-testid` stays the convention to keep test hooks decoupled from the directive-owned aria-wiring `id`.
- WebKit-specific `test.fixme()` is reserved for cross-browser bugs the library still owes a fix for (e.g. modal Dialog return-focus race vs `inert`). Don't add a fixme without a clear comment naming the underlying library issue — the suite's value is exposing those, not papering over them.

Adding a primitive's E2E coverage is part of the workflow: open a route, build a small fixture, and write the focus / keyboard / dismissible specs alongside the existing Vitest contract suite.

#### Coverage tiers — when a new primitive needs E2E

The audit in [#255](https://github.com/tutkli/forty-cdk/issues/255) canonicalised a two-axis decision for whether a primitive owes a Playwright suite. Ask both questions: does the primitive exercise (a) layout / pointer-capture / pointer-position math (jsdom returns zeros for `getBoundingClientRect` / `offset*` / `client*` / `scroll*`, does not run CSS, and mis-models `setPointerCapture`), or (b) focus / keyboard / roving-tabindex / Escape-stack semantics (jsdom mis-models `document.activeElement`, `inert`, and the focus-event order)? If the answer to either is yes, add an E2E spec. If both are no, Vitest is the contract layer and an E2E spec would only add noise.

**The tier is a judgment, so this section carries examples rather than a roster.** Which primitives own a suite today is answered by `ls projects/forty-cdk-harness/e2e/` — 70-plus specs, several per primitive (`table`, `table-virtualized`, `table-reorder`, …) — and a hand-copied list of them here would rot within a release, exactly as it did before [#1401](https://github.com/tutkli/forty-cdk/issues/1401). Read the tiers as the decision procedure; read the directory for the inventory.

- **Tier A — geometry / pointer-capture.** Anything that derives values from `getBoundingClientRect`, `clientWidth` / `scrollWidth`, `setPointerCapture`, `IntersectionObserver` outcomes, or CSS custom properties populated at runtime from measured dimensions. Canonical members: `slider`, `scroll-area`, `pane-resizer` (the focusable drag-resize divider), `drawer` and `toast` (both on the swipe-dismiss helper), `carousel` (the third caller of it), `drag-drop` / `free-drag`, and every virtualization suite (`virtualization`, `listbox-virtualized`, `table-virtualized`, `for-table-body-measured`, …). Templates: [slider.e2e.ts](projects/forty-cdk-harness/e2e/slider.e2e.ts) for pointer-driven thumb / track math; [scroll-area.e2e.ts](projects/forty-cdk-harness/e2e/scroll-area.e2e.ts) for `ResizeObserver`-driven thumb sync; [pane-resizer.e2e.ts](projects/forty-cdk-harness/e2e/pane-resizer.e2e.ts) for the focusable drag-resize divider.
- **Tier B — focus / keyboard / roving.** Anything that owns tab order, roving-tabindex, modal focus trap, return-focus, or layered Escape-stack semantics. Canonical members: `accordion`, `tabs`, `disclosure`, `menubar`, `menu-base`, `menu-sub`, `radio-group`, `toolbar`, `navigation-menu`, `stepper`, `pagination`, `toggle-group`, `tree`, `table` (2D roving grid), the date / time field and picker family, `otp-input`, plus every overlay (Dialog, Popover, DropdownMenu, ContextMenu, Combobox, Tooltip, HoverCard, Select, Listbox, Drawer, Toast). Templates: [listbox.e2e.ts](projects/forty-cdk-harness/e2e/listbox.e2e.ts) for roving-tabindex; [dialog.e2e.ts](projects/forty-cdk-harness/e2e/dialog.e2e.ts) for modal focus trap + return-focus; [nested.e2e.ts](projects/forty-cdk-harness/e2e/nested.e2e.ts) for the popover-inside-dialog Escape-stack contract.
- **Tier C — pure state / form-value / display-only.** No layout-driven logic, no pointer-capture, no roving. Vitest is the contract layer: no E2E spec, no harness fixture, no `/checkbox`-style route. The tier is small and stable enough to enumerate, and it is the one list here that is exhaustive — the eighteen entry points with no `e2e/` spec: `aspect-ratio`, `avatar`, `breadcrumbs`, `breakpoints`, `checkbox`, `field`, `fieldset`, `input`, `meter`, `number-input`, `progress`, `search`, `separator` (a static, non-focusable ARIA divider — the focusable resizer is the separate `pane-resizer` primitive), `switch`, `visually-hidden`, plus the three non-primitive entries `core`, `shared` and `internationalized-date`. Note `toggle` is _not_ in it: `[forToggle]` alone would be, but `toggle-group.e2e.ts` covers the entry point's roving group.

When in doubt: if your draft Vitest spec would have to stub `*.prototype.getBoundingClientRect`, fake `setPointerCapture`, or hand-roll a `document.activeElement` shim, that's the signal — write a Playwright spec instead. See [#193](https://github.com/tutkli/forty-cdk/issues/193) for the cross-platform fragility of layout stubs in jsdom and rule 8 in `### Test isolation — non-negotiables` above for the explicit ban on geometry assertions in Vitest.

#### Mobile coverage — `@mobile` tag

Touch-specific behaviour that desktop projects cannot exercise — swipe-dismiss in Drawer / Toast, long-press → context menu, hover-on-touch fallback on Tooltip / HoverCard, synthetic-touch thumb drag in Slider / Pane resizer / ScrollArea — lives behind the `@mobile` tag added in [#257](https://github.com/tutkli/forty-cdk/issues/257). The convention:

- `playwright.config.ts` declares four projects: `chromium`, `webkit` (desktop) plus `Mobile Chrome` (Pixel 5) and `Mobile Safari` (iPhone 12). Both mobile projects set `hasTouch: true` and `grep: /@mobile/` — they only run tests whose title contains the literal string `@mobile`. Adding the tag to a `test.describe(...)` title applies to every `test(...)` inside.
- Desktop projects (`chromium`, `webkit`) have no `grep` filter, so they run **every** spec including the `@mobile`-tagged ones. The desktop run is the regression guard: if a `@mobile` block starts depending on a touch-only assumption that leaks into the spec body, the desktop projects catch it. Always keep the desktop run green; do not skip `@mobile` tests on desktop projects unless the behaviour is genuinely mobile-only (e.g. `locator.tap()` throws without `hasTouch: true`, in which case gate the assertion behind `isMobileProject(testInfo)` and use `test.skip(!isMobileProject(testInfo), ...)`).
- Use `isMobileProject(testInfo)` (from `_helpers.ts`) to branch the test's expectations when behaviour genuinely differs between mouse and touch. The default shape is to keep the assertion identical across all four projects.
- Pointer-capture-using primitives (Slider thumb, Pane resizer, ScrollArea thumb, Drawer / Toast surface) can drive the gesture two ways: `dragFrom` / `dragFromSteps` with `testInfo` routes to a synthetic-touch `dispatchEvent` path on mobile projects (used by [slider.e2e.ts](projects/forty-cdk-harness/e2e/slider.e2e.ts) and [drawer.e2e.ts](projects/forty-cdk-harness/e2e/drawer.e2e.ts)'s swipe-dismiss block), or raw `page.mouse.move` / `down` / `up` which Playwright's mobile-project emulation on `hasTouch: true` translates to `pointerType: 'touch'` events while preserving the browser's pointer-capture forwarding (used by [drawer.e2e.ts](projects/forty-cdk-harness/e2e/drawer.e2e.ts)'s flick-velocity block, which needs deterministic `dt` between consecutive moves). Pick the synthetic path for clarity when the helper's velocity / arming defaults already model the gesture you want, and the raw `page.mouse.*` path when you need fine-grained control over event timing.

#### Shared E2E helpers ([\_helpers.ts](projects/forty-cdk-harness/e2e/_helpers.ts))

Specs import from `./_helpers.ts` rather than redeclaring inline helpers — [#256](https://github.com/tutkli/forty-cdk/issues/256) extracted the patterns that recurred across drawer / slider / scroll-area / context-menu / tooltip specs into a single module so the touch-branching and roving-tabindex logic stays in one place.

| Helper                                           | What it does                                                                                                                                                                                                                                                                                              |
| ------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `el(page, testid)`                               | Locator for `[data-testid="<testid>"]`. Prefer this over `#id`: a static `id` on a `[id]`-binding directive is now preserved (adopted, not shadowed — [#659](https://github.com/tutkli/forty-cdk/issues/659)), but `data-testid` keeps test hooks decoupled from the directive-owned aria-wiring `id`.    |
| `gotoFixture(page, route, query?)`               | Navigate to `/<route>` with optional `?key=value` query flags consumed by the fixture for per-test configuration (`vetoOpen`, `drawerHeight`, …). Waits for `networkidle` so lazy-loaded fixture chunks finish under the dev server.                                                                      |
| `tabN(page, n, key?)`                            | Press `Tab` (or `Shift+Tab`) `n` times. The default `key` is `'Tab'`; pass `'Shift+Tab'` for backwards traversal.                                                                                                                                                                                         |
| `clickOutside(page)`                             | Click at viewport `(2, 2)` to dismiss overlays. Reliable because every fixture is anchored under `<app-root>` and that coordinate lands in the surrounding body region.                                                                                                                                   |
| `isMobileProject(testInfo)`                      | `true` on `Mobile Chrome` / `Mobile Safari` projects; `false` on the desktop projects. Used by the drag helpers to branch onto a synthetic-touch path and by any `@mobile`-tagged spec that needs to special-case touch behaviour.                                                                        |
| `dragFrom(page, start, delta, opts?)`            | Pointer drag from the centre of `start` by `(dx, dy)` with a 5-px arming step past the swipe-dismiss helper's 4-px arm distance. Default `stepDelayMs: 250` keeps release-time velocity below `FLICK_VELOCITY_PX_PER_MS` (0.4 px/ms). Pass `opts.testInfo` to enable the touch branch on mobile projects. |
| `dragFromSteps(page, start, step, steps, opts?)` | Multi-step variant that exercises the directive's cumulative-offset integrator: arming step plus `steps` equal `step` moves with `stepDelayMs` between them. Default `stepDelayMs: 50` with a 30-px step crosses the flick velocity threshold; raise the delay for the no-flick branch.                   |
| `longPress(locator, ms?)`                        | Touch `pointerdown` → wait → `pointerup` at the locator's centre. Default 600 ms — comfortable margin over Chromium / WebKit's ~500 ms synthetic-`contextmenu` threshold for ContextMenu mobile coverage.                                                                                                 |
| `rovingFirst(page, testid, maxAttempts?)`        | Press `Tab` until `document.activeElement` exposes the matching `data-testid`. Default `maxAttempts: 20`; throws on miss with a diagnostic naming the last-focused testid so a regression surfaces as a clear failure rather than a Playwright timeout.                                                   |
| `expectFocused(locator)`                         | Thin wrapper around `expect(locator).toBeFocused()` so specs read as `await expectFocused(el(page, 'first'))`. Does not await internally so the assertion participates in Playwright's auto-retry.                                                                                                        |
| `expectDeepFocused(page, testid)`                | Same assertion resolved through open shadow roots — `toBeFocused()` reports the shadow **host**, so it cannot tell two controls inside one web component apart. Use it for the composed-tree focus posture ([#1586](https://github.com/tutkli/forty-cdk/issues/1586)); polls, so it retries.              |
