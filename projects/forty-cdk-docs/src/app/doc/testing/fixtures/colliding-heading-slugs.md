# Colliding heading slugs

Headings that slugify to the same string, at one level and across two.

## API

The root's own members.

### API

The trigger's members, under a heading that slugifies the same way.

## Keyboard

## Keyboard
